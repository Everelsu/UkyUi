# Gradle
.gradle/
build/

# RetroFuturaGradle unpacks and decompiles Minecraft into these — hundreds of
# megabytes, all of it regenerable from the build script.
run/
!run/mods/.gitkeep

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
out/

# OS
.DS_Store
Thumbs.db

# Local overrides. gradle.properties is tracked because it pins the toolchain, but
# a machine-specific copy should not be.
gradle-local.properties
local.properties
