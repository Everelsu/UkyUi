# UltraKill Yourself UI — мод интерфейса для 1.12.2

Заменяет ванильные экраны Minecraft на собственные: загрузочный экран, главное
меню, настройки, меню паузы. Всё рисуется кодом (без текстур виджетов), палитра
и поведение задаются конфигом — под техсборку.

Тулчейн: **RetroFuturaGradle** (умеет 1.12.2 наравне с 1.7.10, в отличие от
устаревшего ForgeGradle 2.3).

Все настройки — в [CONFIG.md](CONFIG.md): ссылки с иконками, подсказки на
загрузке, сцена смерти, палитра.

## Требования
- JDK 8 для самого мода (RFG подтянет тулчейн сам)
- **JDK 25 для демона Gradle** — плагин RFG собран под него.
  В `gradle.properties` проекта это прописано, но глобальный
  `~/.gradle/gradle.properties` имеет приоритет и может перебивать значение.
  Если сборка падает с `UnsupportedClassVersionError`, запускай так:
  ```
  ./gradlew build -Dorg.gradle.java.home="C:\Program Files\Eclipse Adoptium\jdk-25.0.3.9-hotspot"
  ```
- Интернет-доступ к `nexus.gtnewhorizons.com`, `maven.cleanroommc.com` и
  стандартным Maven/Mojang/Forge репозиториям
- **MixinBooter** в сборке (в 1.12.2 он заменяет UniMixins): мод — coremod и без
  него mixin-конфиг не применится

## Сборка и запуск
```
./gradlew build
```
```
./gradlew runClient
```

Собирается два джарника: `ukyui-1.7.10-<версия>.jar` — тот, что ставится в сборку,
и `-dev` — деобфусцированный, только для разработки.

## Релизы

Публикация на Modrinth и CurseForge выведена в репозиторий: версия, файл и список
изменений берутся отсюда, а не вбиваются в две формы руками.

```
git tag v0.5.2 && git push origin v0.5.2
```

Тег запускает [`.github/workflows/release.yml`](.github/workflows/release.yml): сборка,
сверка тега с версией в `build.gradle.kts`, загрузка на обе площадки и релиз на GitHub
с приложенным джарником. Список изменений — секция этой версии из
[CHANGELOG.md](CHANGELOG.md), одна и та же во всех трёх местах.

Ручной запуск из вкладки Actions даёт по галке на каждую площадку — Modrinth,
CurseForge, GitHub — и грузит только туда, где отмечено. Это способ дожать релиз,
у которого одна площадка приняла файл, а другая отказала: выгрузку не отозвать, и
повторная отправка делает дубль, а не исправление.

Идентификаторы проектов лежат в `gradle.properties` (они публичные), токены — в
секретах репозитория `MODRINTH_TOKEN` и `CURSEFORGE_TOKEN`. Что где взять и как
опубликовать вручную — [RELEASING.md](RELEASING.md). Тексты страниц площадок —
`store/`.

## Структура
```
src/main/java/com/console/uky/
  UkyUI.java                    — точка входа (@Mod)
  core/UkyCore.java             — FML core plugin, подключает mixin-конфиг
  core/LateMixins.java          — @LateMixin: конфиг миксинов в чужие моды, отдаётся
                                  уже после того, как их jar'ы в classpath
  config/UiConfig.java          — весь конфиг: экраны, эффекты, палитра, сплэш
  proxy/                        — Common/Client прокси

  client/render/
    Draw.java                   — примитивы: заливки, градиенты, круги, свечение,
                                  текстуры с cover-фитом, виньетка, scissor
    Ease.java                   — кривые сглаживания + framerate-independent approach
    Theme.java                  — палитра, производная от UiConfig
    Icons.java                  — глифы из примитивов (play, chat, heart, star, …)
    LinkIcons.java              — иконки ссылок: свои PNG из config/uky/icons
                                  либо подбор по адресу
    AmbientParticles.java       — пыль/угли/искры на фоне меню

  client/gui/
    MenuScreen.java             — база всех экранов: тайминг, fade-in, фон, оверлей
    widget/MenuButton.java      — кнопка (стили PRIMARY/NORMAL/DANGER/GHOST)
    widget/LinkButton.java      — ссылка в главном меню: иконка + подпись
    widget/MenuSlider.java      — слайдер для GameSettings.Options
    widget/MenuOptionButton.java— переключатель с анимированным тумблером
    screen/GuiTitleScreen.java  — главное меню
    screen/GuiOptionsScreen.java— настройки
    screen/GuiPauseScreen.java  — меню паузы

    BlackHole.java              — процедурная чёрная дыра: линзирование, диск, фотонное кольцо

    screen/GuiDeathScreen.java  — сцена смерти: блэкаут, VHS, глитч-заголовок,
                                  без кнопок — любая клавиша или авто-возврат
    screen/GuiStartupQueryScreen.java — вопрос FML посреди загрузки мира
                                  («в сейве нет блоков, продолжить?») в нашем стиле

  client/gui/screen/TitleIntro.java — интро: темнота → удар → формирование дыры

  client/death/
    DeathTheme.java             — палитра, аудио-веса и строки под причину смерти
    DeathTracker.java           — угадывает причину по состоянию клиента
    DeathScene.java             — состояние одной смерти, переживает пересоздание экрана

  client/sound/UkySounds.java   — звуковые события мода (удар, музыка меню, клик)
  client/sound/DeathLoop.java   — сердцебиение и дыхание (громкость/питч по ходу сцены)

  client/splash/
    UkySplash.java              — свой загрузочный экран (форк FML SplashProgress)
    SplashTexture.java          — загрузка текстур напрямую в GL
    SplashFont.java             — FontRenderer до инициализации ресурсов

  client/mods/
    ModConfigCatalog.java       — какие моды вообще имеют экран настроек
    AngelicaOptions.java        — настройки рендерера как модель, вкладка Graphics
    VideoSettingsTakeover.java  — запасная дверь к чужому экрану видео
    WailaPanel.java             — подложка подсказки Waila нашей панелью
    WailaTooltips.java          — виджет прогресса внутри подсказки
    WailaMining.java            — прогресс ломания блока для полоски на кромке
    QuestBookAtlas.java         — лист виджетов BetterQuesting, рисуется из палитры
    QuestBookTheme.java         — регистрация и выбор темы в книге квестов
    QuestBookTransition.java    — затемнение под книгой и её появление

  client/config/                — экран конфига в списке модов (FML GuiConfig)
  handler/                      — подмена ванильных экранов, реакция на смену конфига
  mixins/MixinSplashProgress.java — передаёт загрузочный экран нашему коду
  mixins/mods/MixinWailaOverlay.java — перехватывает отрисовку коробки Waila

src/main/resources/
  mcmod.info
  mixins.uky.early.client.json
  mixins.uky.mods.client.json   — миксины в чужие моды, required=false
  assets/uky/lang/              — только те строки, которых нет в ванилле
  assets/uky/textures/gui/background.png   — ЗАГЛУШКА, заменить на арт сборки
```

## Чёрная дыра

Рисуется кодом (`BlackHole.java`).

Линзирование считается аналитически. Полное интегрирование геодезических (как в
CUDA-рейтрейсерах) на кадр не потянуть, но для точечного источника у
шварцшильдовской линзы есть замкнутое решение, а звёздное небо — это и есть
точки. Для звезды на угловом расстоянии `b` от сингулярности при радиусе
Эйнштейна `Te`:

```
T+ = (b + sqrt(b² + 4Te²)) / 2     основное изображение, снаружи кольца
T- = (b - sqrt(b² + 4Te²)) / 2     вторичное, зеркальное внутри кольца
усиление = |1 / (1 - (Te/T)⁴)|
```

Это один корень на звезду, и оно даёт настоящие эффекты: звёзды обтекают тень,
двоятся, и вспыхивает кольцо Эйнштейна, когда звезда проходит за дырой.

Аккреционный диск — наклонённое кольцо, затенённое value-noise турбулентностью,
с релятивистским бимингом (сторона, летящая на зрителя, ярче). Плюс почти
круглое вторичное кольцо — это свет с дальней стороны диска, загнутый над тенью.

**Падающая материя.** Отдельный набор звёзд по спирали затягивается внутрь.
Это не интегрирование орбит (либо мелкий шаг, либо накопление ошибки), а прямая
запись того, как ведёт себя распадающаяся орбита: угловая скорость растёт как
`1/r^1.5` (Кеплер), радиальный снос разгоняется как `1/r²`. Получается широкая
медленная дуга, которая на последних радиусах срывается в тугую спираль — глаз
читает это как «затягивает». По дороге орбита выпрямляется в плоскость диска
(так и ведёт себя реальная аккреция), звезда растягивается приливными силами в
штрих, краснеет от гравитационного красного смещения, вспыхивает и пропадает на
границе тени. Половины делятся относительно тени, поэтому звезда, проходящая за
дырой, действительно перекрывается ей.

Цвета диска берутся из `theme.accent` / `theme.accentAlt`, так что дыра
подстраивается под палитру сборки.

## Звук и интро

`assets/uky/sounds.json` регистрирует:
- `uky:intro_impact` (`low.ogg`) — удар в интро
- `uky:menu_music` (`music.ogg`) — зацикленная музыка меню
- `uky:button` (`button_sound.ogg`) — клик по кнопке
- `uky:delete_hold` / `uky:delete_break` — держание кнопки удаления мира
- `uky:heartbeat` / `uky:breathing` — под сценой смерти, `DeathLoop.java`
  подкручивает громкость и питч по ходу сцены (замедляющееся сердце, нарастающее
  дыхание), а не просто проигрывает файл целиком

Интро играет один раз за запуск игры при первом открытии главного меню:
темнота → удар со звуком, вспышка и ударная волна → дыра раскручивается →
проявляется меню. Пропускается любым кликом или клавишей — но не в первые 0.3 с:
сборка грузится минутами, в неё принято кликать от нечего делать, а LWJGL отдаёт
весь накопленный буфер первому экрану, который спросит ввод. Этим экраном
оказывается как раз титульный, и интро пропускалось кликом, сделанным задолго до
его начала.

«Сыграно за сессию» отмечается на ударе, а не при создании экрана: главное меню в
большой сборке конструируется не один раз, и экран, который построили и выкинули
до первого кадра, не должен тратить единственное интро. Если интро всё-таки не
играет, в логе есть ровно одна строчка `Title intro: ...` с причиной.

Загрузочный экран звук не играет: он работает до инициализации звуковой
системы Minecraft.

## Компоновка главного меню

`mainmenu.layout`:
- `left` (по умолчанию) — леттеринг сверху по центру, меню текстовой колонкой
  слева, чёрная дыра занимает правые две трети. Кнопки без панелей, цвет из
  `theme.accent`, «Выйти» — из `theme.danger`.
- `center` — всё стопкой по центру, кнопки с панелями.

`mainmenu.links` — ряд ссылок под меню, по одной записи в строке:
```
Подпись|https://example.com
Подпись|https://example.com|иконка
```
Третье поле необязательное. Без него иконка подбирается по адресу (YouTube,
Discord, Telegram, Boosty, Twitch, Patreon распознаются отдельно, остальное —
по грубым категориям: видео / чат / донат / репозиторий / прочее). Иконки —
стилизованные марки, нарисованные кодом (`Icons.java`, `LinkIcons.java`), либо
свой PNG из `config/uky/icons/` (имя файла третьим полем). Длинный список сам
переносится на вторую строку. Клик открывает ванильное окно подтверждения
перехода, затем системный браузер. Подробности и полный список ключевых слов
иконок — в [CONFIG.md](CONFIG.md).

## Экран смерти

`GuiDeathScreen` подменяет `GuiGameOver`. Без кнопок: картинка гаснет, идёт
VHS-повреждённая сцена (сканлайны, рвущаяся полоса, дропауты, зерно, распадающийся
на мусорные символы заголовок), под ней сердцебиение и дыхание. Дальше — либо
любая клавиша/клик, либо авто-возврат по таймеру. Выход не мгновенный: плёнку
рвёт сильнее, заголовок разваливается, картинка схлопывается в яркую полоску и
гаснет — и только тогда уходит пакет респавна.

Причина смерти угадывается на клиенте (`DeathTracker`) по последнему известному
состоянию игрока — сервер её не присылает. От неё зависят палитра сцены
(`DeathTheme`), баланс звуков и фраза из `assets/uky/lang/*.lang`
(`uky.death.<причина>.<номер>`). Состояние одной смерти (`DeathScene`) переживает
пересоздание экрана, которое ваниль делает, пока респавн-пакет летит на сервер.

Настройки — категория `death` в конфиге, подробно в [CONFIG.md](CONFIG.md).

## FML-диалоги посреди загрузки мира

Экран вроде «в сейве отсутствуют блоки — продолжить?» рисуется не игровым циклом:
пока стартует мир, клиентский поток стоит в ожидании сервера, и единственное, что
его отрисовывает и читает ввод — `FMLClientHandler.handleLoadingScreen`, вызванный
из `LoadingScreenRenderer.setLoadingProgress`. `UkyLoadingScreen` перехватывает
именно этот путь, поэтому такой вопрос дополнительно прокручивается на 60 fps в
`UkyLoadingScreen.pumpStartupQuery`, пока не получит ответ — иначе он рисовался бы
на паре кадров в секунду и не видел бы кликов. `GuiStartupQueryScreen` — тот же
экран в нашем стиле: заголовок отдельно от списка, список скроллится, длинные
строки переносятся, Escape отвечает «нет», а не вешает загрузку.

## Чужие моды

Мод перерисовывает интерфейсы нескольких сторонних модов, если они есть в сборке.
Ни один из них не является зависимостью: ничего не компилируется против их
классов, отсутствие мода — обычный случай, а не ошибка. Выключается в конфиге,
секция `mods`.

**Waila** — подсказка о блоке, на который смотришь. Заменяется ровно подложка:
Waila сама меряет коробку и сама рисует в неё содержимое, а рисование фона
уходит нам (mixin в `OverlayRenderer.drawTooltipBox`, `@Pseudo`, конфиг отдаётся
через `@LateMixin` — см. `core/LateMixins.java`, там же написано, почему обычные
два способа тут не работают). Получается та же панель, что
у списка игроков по Tab: тёмная заливка, волосяная рамка, золотой рельс слева.
Всё, что дописывают в подсказку другие моды, остаётся на месте — этот код не
знает, что там написано.

Появление панели — только украшение поверх: сама плашка рисуется сразу и целиком,
а анимируются рельс (прочерчивается сверху вниз) и один блик по верхней кромке.

Так было не сразу. Сначала панель прорисовывалась слева направо, а текст Waila
обрезался по движущейся кромке через `glScissor`, чтобы содержимое приезжало вместе
с ней. Читалось это не как анимация, а как поломка: 0.16 с на такой сборке — это
четыре кадра, а четыре кадра жёсткой кромки, едущей по полурисованным глифам, дают
дёрганье, а не проявление. Всё, что двигает содержимое HUD-элемента, который игрок
в этот момент читает, должно окупаться сильнее.

Виджет прогресса внутри подсказки — тоже наш. Waila ищет их в обычной карте по
имени (`waila.progress`), так что достаточно положить туда своё: рисуется через
`java.lang.reflect.Proxy`, потому что интерфейс существует только вместе с Waila.
Размер отдаём тот же (32x16) — по нему считается вся раскладка подсказки.

**BetterQuesting** — книга квестов рисуется целиком через тему: панель, кнопка,
скроллбар, узел квеста и цвет текста берутся по имени из активной темы. Поэтому
книгу не нужно перерисовывать — ей нужно дать другую таблицу. Тема собирается в
рантайме из `Theme`: лист виджетов (9-slice, 256×256) рисуется в `BufferedImage`
на `DynamicTexture` при первом открытии книги, цвета ставятся оттуда же. Смысл
именно в этом: палитра живёт в конфиге, и обещание «поменял акцент — перекрасился
весь интерфейс» шитая в ресурсы PNG-тема не выполняла бы ровно на том экране, где
живёт квест-пак. Всё делается рефлексией, любая ошибка — строчка в лог и книга в
своём обычном виде.

Тема видна в списке тем самой книги под именем мода, так что её можно выбрать и
руками; `mods.restyleQuestBook` решает только, выбирается ли она сама.

Отдельно от темы — затемнение мира под книгой и её появление за 0.2 с
(`QuestBookTransition`, ключ `mods.questBookTransition`). Вешается на форджевые
`DrawScreenEvent.Pre/Post`, сама BetterQuesting об этом не знает. Масштаб намеренно
почти незаметный: книга режет свои списки через `glScissor`, а тот работает в
пикселях окна и за масштабом модельвью не идёт. Обратный ход несимметричен —
затемнение снимается уже из render-тика, после того как экран закрылся, потому что
рисовать сами панели в этот момент неоткуда.

## Ассеты
- Мод не поставляет ни фон, ни логотип — только шрифт и звуки.
- `assets/uky/textures/gui/logo.png` — **опционально**. Если файла нет, и в меню,
  и на загрузочном экране рисуется текстовый леттеринг из `mainmenu.title`.
- `assets/uky/textures/gui/background.png` — нужен только при
  `effects.background=image`. Если файла нет, режим тихо откатывается на `solid`.
- Загрузочный экран смотрит в `config/uky-splash/background.png` и
  `config/uky-splash/logo.png`. По умолчанию там ничего нет и экран чисто чёрный —
  это и есть задуманный вид; картинка появится только если её туда положить.
- `config/uky/icons/` — картинки для ссылок главного меню (см. ниже и
  [CONFIG.md](CONFIG.md)). Создаётся сама при первом запуске.

## Конфиг
`config/uky/uky.cfg` (у себя же, рядом с `config/uky/icons/`), категории:
- `screens` — какие ванильные экраны подменять, включая экран смерти
- `mods` — интерфейсы чужих модов: подсказка Waila, тема книги квестов
  BetterQuesting
- `death` — длительность сцены, авто-возврат, сила VHS-эффектов, звук
- `effects` — `background` (`blackhole` / `image` / `solid`), частицы, виньетка,
  зерно, scanlines, дрейф фона, звуки кнопок
- `mainmenu` — какие пункты показывать, леттеринг, подпись, футер (левый и
  правый), `intro` (интро с ударом), `music`, `musicVolume`, ссылки и папка
  их иконок
- `splash` — включён ли свой загрузочный экран, показывать ли подсказки, сами
  подсказки (ship'ится пять примеров, не пустой список)
- `theme` — 7 базовых цветов `0xRRGGBB`, из них выводится вся остальная палитра

Все значения по умолчанию объявлены **только** в полях `UiConfig` — не
дублируются при чтении, чтобы правка дефолта не расходилась с тем, что реально
пишется в файл. Файл переписывается при каждом запуске: Forge считает конфиг
изменившимся только когда поменялось значение, а не комментарий, поэтому иначе
перевод/переформулировка комментария не долетала бы до уже существующих
установок. Значения при этом не трогаются, обновляется только текст.

Старый `config/uky.cfg` (до переноса в подпапку) переносится в новый путь
автоматически при первом запуске, значения сохраняются.

Загрузочный экран читает конфиг сам (до pre-init), поэтому файл лежит по
фиксированному пути.

Если загрузочный экран глючит на какой-то видеокарте — `splash.customSplash=false`,
игра вернётся к форджевому. Любая ошибка внутри нашего сплэша тоже приводит к
откату на форджевый, а не к падению игры.

Полное описание всех ключей, с примерами — [CONFIG.md](CONFIG.md).

## Что дальше
- Переработка системы получения достижений и отвязка их получения от определённой последовательности
- Переработка hud внутри игры. Потенциально...
- Собственный экран настроек Waila вместо её встроенного (сейчас перерисована
  только подсказка в игре)
- Фикс багов. Я ваши Tips в загрузке мира в рот шатал что за мод то блять?
- Абобааааа заброшу проектик на неделю. уж точно.
