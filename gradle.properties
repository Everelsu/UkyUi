org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-25.0.3.9-hotspot

# ---- Publishing ----
#
# Which project each store upload goes to. Both are public identifiers, not secrets,
# which is why they live here; the tokens do not, and are read from the environment.
# See RELEASING.md.
#
# Modrinth takes the id or the slug from the project's URL — modrinth.com/mod/<slug>.
# CurseForge takes the numeric "Project ID" shown in the right-hand column of the
# project page; its slug is not accepted by the upload API.
modrinthProjectId=Dhk4uKiR
curseforgeProjectId=1647987
